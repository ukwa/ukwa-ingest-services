version: '2'

services:
  # UKWA Heritrix common setup
  ukwa-heritrix:
    image: ukwa/heritrix
    environment:
      - "LC_ALL=en_US.utf8"
      - "HERITRIX_USER=${HERITRIX_USER}"
      - "HERITRIX_PASSWORD=${HERITRIX_PASSWORD}"
    #  - "MONITRIX_ENABLED=1"
    extra_hosts:
      - "clamd:192.168.45.87"
